{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets|images|videos|manifest|robots|sitemap).*)",
      "destination": "/index.html"
    }
  ]
}